name: Apply reviewed rc2 patch

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  paused:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Applicator paused while the reviewed source-only bundle is replaced."
