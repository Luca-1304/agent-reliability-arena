name: Apply reviewed rc2 source patch

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  paused:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Applicator paused while the final whitespace-clean bundle is repinned."
