# Final v0.1.0 GitHub CI verification

This documentation-only commit triggers the permanent Python 3.10–3.13 matrix from the complete published tree after the digest-verified per-scenario reference evidence was restored.

No runtime source, test, verifier, metric, fixture, reference artifact, or static-viewer behaviour is changed.

Required checks include source tests, release verification, all installed commands, clean-wheel tests, deterministic artifact equality, replay, public export, and dependency validation.
